<Project ToolsVersion="16.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <AssemblyName>$(AssemblyName).Sync.$(MSBuildProjectName)</AssemblyName>
    <RootNamespace>$(RootNamespace).Sync.$(MSBuildProjectName)</RootNamespace>
    <OutputPath>$(OutputPath)$(AssemblyName)\</OutputPath>
  </PropertyGroup>

</Project>