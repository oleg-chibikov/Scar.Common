<Project ToolsVersion="16.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <AssemblyName>$(AssemblyName).ImageProcessing.$(MSBuildProjectName)</AssemblyName>
    <RootNamespace>$(RootNamespace).ImageProcessing.$(MSBuildProjectName)</RootNamespace>
    <OutputPath>$(OutputPath)$(AssemblyName)\</OutputPath>
  </PropertyGroup>

</Project>